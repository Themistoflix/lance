import 'package:flutter/material.dart';

class SwipeSelectionScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Image(image: AssetImage('assets/margherita.jpg')),
      ),
    );
  }
}